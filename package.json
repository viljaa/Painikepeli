{
  "name": "painikepeli",
  "version": "1.0.0",
  "description": "Realtime button multiplayer browser game.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "heroku-postbuild": "cd painikepeli-client && npm install && npm run build"
  },
  "author": "Ville Jaatinen",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
